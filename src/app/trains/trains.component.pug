svg([attr.width]='width', [attr.height]='height')
    g(*ngFor='let train of trainsData; let i = index', [attr.transform]="'translate(0 ' + (20 + (i * spacing)) + ')'")
        g(app-train, [data]='train', [i]='i+1')
